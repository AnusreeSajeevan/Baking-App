<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">
    <TextView
        android:textAlignment="center"
        android:textStyle="bold"
        android:background="@color/colorPrimary"
        android:textColor="@color/colorWhite"
        android:padding="@dimen/margin_20"
        android:id="@+id/recipe_name_widget"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />
    <ListView
        android:layout_below="@+id/recipe_name_widget"
        android:id="@+id/list_view_widget"
        android:gravity="center"
        android:layout_width="match_parent"
        android:numColumns="4"
        android:layout_height="match_parent"/>
    <RelativeLayout
        android:id="@+id/layout_empty"
        android:layout_width="match_parent"
        android:layout_height="match_parent">
        <ImageView
            android:layout_centerInParent="true"
            android:alpha=".2"
            app:srcCompat="@drawable/ic_restaurant_icon"
            android:layout_width="100dp"
            android:layout_height="100dp"
            android:contentDescription="image to be shown if no recipes added" />
        <TextView
            android:layout_centerInParent="true"
            android:text="@string/no_recipes_added"
            android:textColor="#ffffff"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" />
    </RelativeLayout>
</RelativeLayout>